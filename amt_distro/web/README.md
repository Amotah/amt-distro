# AMT Distro — Web (Next.js + Tailwind)

This is a minimal Next.js frontend scaffolded to provide a modern Home page.

Quick start

```bash
cd web
npm install
npm run dev
```

Open http://localhost:3000
